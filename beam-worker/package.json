{
  "name": "beam-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Beam cloud transfer mode — Gofile.io direct upload, KV tokens, streaming download",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
